# Selvedge

> Selvedge is a local MCP server and CLI for recording code decisions, stated reasoning, and rejected approaches. Selvedge's log_change writes events; Selvedge's prior_attempts retrieves recorded attempts and outcomes before a later edit. SQLite storage, MIT licensed, no LLM in the core. Capture and retrieval require the agent or user to call the tools.

The diff is git's job. The *why* is Selvedge's. Reasoning is captured live, by the agent, in the same context that produced the change — not inferred post-hoc from the diff by a second model. Install with `pip install selvedge`, then `selvedge setup`.

## Start here
- [Home](https://selvedge.sh): what Selvedge is, who it's for, the quickstart.
- [Quickstart](https://selvedge.sh/start/quickstart/): `pip install selvedge`, the `selvedge setup` wizard, wiring into Claude Code / Cursor / Copilot / Codex / Gemini CLI.
- [Agent prompt block](https://selvedge.sh/prompt-block/): the copy-paste `CLAUDE.md` / `.cursorrules` block that tells an agent when and how to call Selvedge. Also emitted by `selvedge prompt`.
- [Stop your agent repeating reverted mistakes](https://selvedge.sh/prior-attempts): case study of the prior_attempts tool — the agent checks an entity's history before editing and changes plan when the change was tried and reverted.
- [The MCP server that tracks why your AI agent changed code](https://selvedge.sh/track-why/): the write path (`log_change`, live, in the agent's own words) and the read path (`blame`, `history`) for decision provenance.
- [Long-term memory for AI coding agents](https://selvedge.sh/agent-memory/): decisions that survive the session — entity-keyed and deterministic, with expiry via `stale_decisions`. Not a vector store.
- [A local decision audit trail for AI-written code](https://selvedge.sh/audit-trail/): who/what/when/why per entity, searchable, exportable as Agent Trace records.
- [How Selvedge compares](https://selvedge.sh/compare/agent-tools/): Selvedge vs. AgentDiff / Origin / Git AI / BlamePrompt — captured-live + entity-level + changesets.
- [Best MCP servers for codebase memory, compared](https://selvedge.sh/compare/codebase-memory-mcp-servers/): the honest map — code intelligence vs. session context vs. general memory vs. decision provenance, and when Selvedge is not the right pick.

- [Revisit a rejected approach](https://selvedge.sh/guides/revisit-a-rejected-approach/): tested CLI walkthrough.
- [Instruction files and ADRs](https://selvedge.sh/compare/instructions-and-adrs/): choose a record format your team will maintain.
- [Codex setup](https://selvedge.sh/mcp/codex/): MCP configuration and project instructions.
- [Gemini CLI setup](https://selvedge.sh/mcp/gemini/): MCP configuration and project instructions.

An empty query result means no qualifying record was found, not that the approach was never tried. MCP clients may send retrieved records to their model providers. Keep private records in a private destination.

## MCP tools (8, over stdio)
- `log_change`: record a change event with entity, diff, and reasoning (pass `rename_from` with `change_type="rename"` for renames).
- `prior_attempts`: prior change attempts on an entity + outcome — was it tried and reverted, or considered and rejected? Stated outcomes (explicit `revert`/`reject` events) report `confidence: "exact"`; proximity inference is the tiebreaker. Call it *before* editing.
- `blame`: most recent change + context for an exact entity.
- `diff`: history for an entity or entity prefix.
- `history`: filtered history across all entities.
- `changeset`: all events grouped under a named feature/task slug.
- `search`: full-text search across all events.
- `stale_decisions`: decisions due for a revisit — a machine-checkable `expires_when` condition fired, `revisit_after` passed while the entity is still in active use (pure age never surfaces), or a later change matched the decision's `stale_when`.

## Concepts
- [Concepts hub](https://selvedge.sh/concepts/): definitions and links to practical guides.
- [AI code provenance](https://selvedge.sh/concepts/ai-code-provenance/): AI code provenance is the recorded origin and history of an AI-assisted code change: the affected code, the people or tools involved, and the evidence linking them to the change.
- [Agent memory](https://selvedge.sh/concepts/agent-memory/): Agent memory is information retained beyond an agent’s current context and made available to later work.
- [Prior attempt](https://selvedge.sh/concepts/prior-attempt/): A prior attempt is an earlier approach considered or implemented for a task or part of a codebase.
- [Entity-level tracking](https://selvedge.sh/concepts/entity-level-tracking/): Entity-level tracking associates a record with an identifiable part of a system, such as a function, database column, route, or dependency.
- [Changeset](https://selvedge.sh/concepts/changeset/): In Selvedge, a changeset is a group of recorded events sharing a changeset identifier for a feature, task, or other unit of work.
- [Stale decision](https://selvedge.sh/concepts/stale-decision/): A stale decision is a recorded choice whose assumptions, constraints, or review conditions may no longer match the current project.
- [Recorded versus inferred reasoning](https://selvedge.sh/concepts/captured-live-vs-inferred/): Recorded reasoning is an explanation explicitly supplied by an agent or person and preserved with a decision.

## Reference
- [README](https://github.com/masondelan/selvedge/blob/main/README.md): full overview, MCP tool table, CLI reference, entity-path conventions.
- [Changelog](https://github.com/masondelan/selvedge/blob/main/CHANGELOG.md): what shipped, per version.
- [Architecture](https://github.com/masondelan/selvedge/blob/main/docs/architecture.md): data model, phase plan, non-goals.
- [Agent Trace interop](https://selvedge.sh/compare/agent-trace/): Selvedge supports Agent Trace v0.1.0 import and export. Verify field preservation in receiving tools.
- [prior_attempts demo](https://github.com/masondelan/selvedge/blob/main/docs/demos/prior-attempts.md): worked transcript of an agent checking history and changing its plan.

## Install
- PyPI: https://pypi.org/project/selvedge/ — `pip install selvedge`
- GitHub: https://github.com/masondelan/selvedge
- Claude Code plugin: `/plugin marketplace add masondelan/selvedge` then `/plugin install selvedge@selvedge`
