---
# AUTO-GENERATED by scripts/gen-seo-pages.mjs from src/data/seo.mjs — do not edit by hand.
title: Add Selvedge to Gemini CLI
description: "Connect Selvedge to Gemini CLI with project MCP settings and GEMINI.md instructions. Save and recall project decisions locally."
structuredData:
  type: howto
  steps:
    - name: "Install Selvedge"
      text: "Run pip install selvedge to put the selvedge-server MCP command and the selvedge CLI on your PATH."
    - name: "Add the MCP server to Gemini CLI"
      text: "Add the selvedge-server command as an MCP server in Gemini CLI (.gemini/settings.json in your project). selvedge setup can also write this for you."
    - name: "Tell your agent to use it"
      text: "Run selvedge prompt --install GEMINI.md to install instructions for log_change and prior_attempts."
    - name: "Verify the connection"
      text: "Restart Gemini CLI in the project. Run /mcp and confirm Selvedge's tools are available. Ask the agent to save a real rejected approach using log_change and retrieve it using prior_attempts in a new session."
---

Run `selvedge setup --agent gemini` (0.3.12+) to write the MCP entry and GEMINI.md instructions. Selvedge stores the reasoning Gemini CLI records and makes it available to subsequent sessions.

## Before you start

`pip install selvedge` gives you the `selvedge` CLI (`selvedge setup`, `selvedge blame`, and the `selvedge prompt` helper below):

```bash
pip install selvedge
```

The MCP config below launches the server with [`uvx`](https://docs.astral.sh/uv/) instead of a bare `selvedge-server`, so it starts reliably even when your editor doesn't share your shell's PATH — it just needs [`uv`](https://docs.astral.sh/uv/getting-started/installation/) installed. Prefer the global binary? Swap `uvx --from selvedge selvedge-server` for `selvedge-server`.

## Add the MCP server

Add this to `.gemini/settings.json` in your project:

```json
{
  "mcpServers": {
    "selvedge": {
      "command": "uvx",
      "args": ["--from", "selvedge", "selvedge-server"]
    }
  }
}
```

Prefer not to touch config files? `selvedge setup` detects Gemini CLI and writes this for you (with a `.bak` first).

## Tell your agent when to call it

Selvedge captures decisions when your agent calls it. Install the instruction block into your project’s GEMINI.md:

```bash
selvedge prompt --install GEMINI.md
```

It's also a copy-paste block on the site: [the Selvedge agent prompt block](/prompt-block/).

## Verify

Restart Gemini CLI in the project. Run `/mcp` and confirm Selvedge's tools are available. Ask the agent to save a real rejected approach using `log_change` and retrieve it using `prior_attempts` in a new session.

## If it doesn't connect

The agent must be able to find `uvx` (manual config) or `selvedge-server` (wizard setup). Approve the server if prompted. Setup writes GEMINI.md so the agent knows when to use Selvedge; it does not install Claude Code lifecycle hooks.

## Next

- [Quickstart →](/start/quickstart/) — the three-command setup and what `selvedge setup` automates.
- [MCP tools →](/reference/mcp-tools/) — the eight tools Gemini CLI will be able to call.
- [Gemini CLI MCP docs →](https://geminicli.com/docs/tools/mcp-server/) — the upstream reference.
