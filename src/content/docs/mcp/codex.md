---
# AUTO-GENERATED by scripts/gen-seo-pages.mjs from src/data/seo.mjs — do not edit by hand.
title: Add Selvedge to Codex
description: "Connect Selvedge to Codex with project MCP configuration and AGENTS.md. Recall rejected approaches and saved decisions in a new session."
structuredData:
  type: howto
  steps:
    - name: "Install Selvedge"
      text: "Run pip install selvedge to put the selvedge-server MCP command and the selvedge CLI on your PATH."
    - name: "Add the MCP server to Codex"
      text: "Add the selvedge-server command as an MCP server in Codex (.codex/config.toml in a trusted project, or ~/.codex/config.toml for user-wide setup). selvedge setup can also write this for you."
    - name: "Tell your agent to use it"
      text: "Run selvedge prompt --install AGENTS.md to install instructions for log_change and prior_attempts."
    - name: "Verify the connection"
      text: "Restart Codex in the trusted project. Enable the Selvedge MCP server in Settings → MCP servers if needed. Ask Codex to record a real rejected approach with log_change, then retrieve it with prior_attempts. Start a new session and query the same entity."
---

Selvedge gives Codex a local store for project decisions and rejected approaches. Run `selvedge setup --agent codex` (0.3.12+) to install project MCP configuration and instructions together.

## Before you start

`pip install selvedge` gives you the `selvedge` CLI (`selvedge setup`, `selvedge blame`, and the `selvedge prompt` helper below):

```bash
pip install selvedge
```

The MCP config below launches the server with [`uvx`](https://docs.astral.sh/uv/) instead of a bare `selvedge-server`, so the package can be fetched on demand — the editor still needs to find [`uv`](https://docs.astral.sh/uv/getting-started/installation/) installed. Prefer the global binary? Swap `uvx --from selvedge selvedge-server` for `selvedge-server`.

## Add the MCP server

Add this to `.codex/config.toml` in a trusted project, or `~/.codex/config.toml` for user-wide setup:

```toml
[mcp_servers.selvedge]
command = "uvx"
args = ["--from", "selvedge", "selvedge-server"]
```

Prefer not to touch config files? `selvedge setup` detects Codex and writes this for you (with a `.bak` first).

## Tell your agent when to call it

Selvedge captures decisions when your agent calls it. Install the instruction block into your project’s AGENTS.md:

```bash
selvedge prompt --install AGENTS.md
```

It's also a copy-paste block on the site: [the Selvedge agent prompt block](/prompt-block/).

## Verify

Restart Codex in the trusted project. Enable the Selvedge MCP server in Settings → MCP servers if needed. Ask Codex to record a real rejected approach with `log_change`, then retrieve it with `prior_attempts`. Start a new session and query the same entity.

## If it doesn't connect

Codex project configuration loads only for trusted projects. The setup wizard writes `.codex/config.toml` and installs instructions in `AGENTS.md`. Custom TOML entries must be reconciled manually. Make sure `uvx` (manual setup) or `selvedge-server` (wizard setup) is on the PATH Codex uses. As of 0.3.15, setup also installs .codex/hooks.json. Review and trust those definitions with /hooks.

## Next

- [Quickstart →](/start/quickstart/) — the three-command setup and what `selvedge setup` automates.
- [MCP tools →](/reference/mcp-tools/) — the eight tools Codex will be able to call.
- [Codex MCP docs →](https://learn.chatgpt.com/docs/extend/mcp?surface=cli) — the upstream reference.
