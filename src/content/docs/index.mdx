---
title: Persistent decision memory for AI coding agents
description: Preserve why code changed and which approaches were rejected. Selvedge gives AI coding agents local decision memory through MCP and CLI.
template: splash
head:
  - tag: meta
    attrs:
      property: og:type
      content: website
lastUpdated: 2026-09-24
hero:
  tagline: Keep the why with your code.
---
import AgentSetup from '../../components/AgentSetup.astro';
import Terminal from '../../components/Terminal.astro';
import { agents } from '../../data/product.mjs';

<div class="agent-strip" aria-label="Supported agents"><span class="agent-strip-label">WORKS WITH</span> {agents.map((agent, i) => <>{i > 0 && <span> · </span>}{agent.shortLabel ?? agent.label}</>)}</div>

[Check compatibility and capabilities](/reference/compatibility/) · [Verify your first decision](/guides/verify-first-decision/)

## Choose your agent

Open a terminal in your project folder. Pick your agent, copy the commands, and follow the setup prompts.

<AgentSetup />

## Try the demo

Want to see it work first? The demo saves a rejection and reads it back through a fresh database connection. It uses a temporary database, so your project stays untouched.

<Terminal title="terminal — install and try" command={"uv tool install --upgrade selvedge\nselvedge demo"} copy kind="install" />

Requires [uv](https://docs.astral.sh/uv/getting-started/installation/) and Python 3.10+. Prefer pip? [Installation instructions →](/start/quickstart/#install)

## What gets remembered

<div class="memory-rows">
  <section><h3>The decision</h3><p>What changed and why. Find it by the function, table, dependency or endpoint it belongs to.</p></section>
  <section><h3>The rejected approach</h3><p>What you considered or rolled back, with the reason. Your next session can check before trying it again.</p></section>
  <section><h3>When to revisit it</h3><p>What would change your mind. Add a review date or supported expiry condition so a past decision can be reconsidered.</p></section>
</div>

Learn to [share decisions between agents](/guides/share-memory-between-agents/), explore [memory across coding sessions](/agent-memory/), [try a recorded rejection and retrieval](/guides/revisit-a-rejected-approach/), or learn how to [keep a decision audit trail](/audit-trail/).

## A small tool that stays with your project

Selvedge is a local MCP server for AI coding agents. Decisions live in `.selvedge/selvedge.db`, a local SQLite file. Selvedge makes no LLM calls and needs no account or subscription. It’s open source under the MIT license.

Connect agents to the same project and database to share the history. Moving to another machine? Carry the database with the project, or export a readable decision log. Your chosen coding agent still follows its own data policy.

Selvedge records what you or your agent explicitly save. The installed instructions help the agent use it; they don’t guarantee that every decision is captured.

## Find Selvedge

Available through the package registry and MCP directories you already use.

<div class="publication-links">
  <a href="https://pypi.org/project/selvedge/"><strong>PyPI</strong><span>Python package ↗</span></a>
  <a href="https://smithery.ai/server/masondelan/selvedge"><strong>Smithery</strong><span>MCP directory ↗</span></a>
  <a href="https://glama.ai/mcp/servers/masondelan/selvedge"><strong>Glama</strong><span>MCP directory ↗</span></a>
  <a href="https://mcpservers.org/servers/masondelan/selvedge"><strong>MCP Servers</strong><span>MCP directory ↗</span></a>
  <a href="https://github.com/masondelan/selvedge"><strong>GitHub</strong><span>Source & releases ↗</span></a>
</div>

<div class="launch-footer"><a href="/start/quickstart/">Documentation →</a><a href="https://github.com/masondelan/selvedge">Source on GitHub ↗</a><a href="/privacy/">Website privacy</a></div>
